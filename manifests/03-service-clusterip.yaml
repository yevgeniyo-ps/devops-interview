apiVersion: v1
kind: Service
metadata:
  name: web-app-clusterip
  namespace: default
spec:
  type: ClusterIP
  selector:
    # ISSUE: Wrong selector - pods have label "app: webapp" not "web-app"
    app: web-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
